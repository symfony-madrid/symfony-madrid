<h1>SFNew list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Title</th>
            <th>Body</th>
            <th>Author</th>
            <th>Published_at</th>
            <th>Photo</th>
            <th>File1</th>
            <th>File2</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('new_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.title }}</td>
            <td>{{ entity.body }}</td>
            <td>{{ entity.author }}</td>
            <td>{% if entity.publishedat %}{{ entity.publishedat|date('Y-m-d H:i:s') }}{% endif%}</td>
            <td>{{ entity.photo }}</td>
            <td>{{ entity.file1 }}</td>
            <td>{{ entity.file2 }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('new_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('new_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('new_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
