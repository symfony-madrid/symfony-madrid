SFMWebsiteBundle:
    resource: "@SFMWebsiteBundle/Controller/"
    type:     annotation
    prefix:   /
    
PollBundle:
    resource: "@PollBundle/Controller/"
    type:     annotation
    prefix:   /

login_check:
    pattern:   /admin/login_check

admin_logout:
    pattern:   /admin/logout

# Internal routing configuration to handle ESI
#_internal:
#   resource: "@FrameworkBundle/Resources/config/routing/internal.xml"
#   prefix:   /_internal
